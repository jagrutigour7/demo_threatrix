description: >
  Install threat agent
parameters:
  oid:
    type: string
    description: First number
  eid:
    type: string
    description: First number
  api_key:
    type: string
    description: First number
  dir:
    type: string
    description: First number
executor: default

steps:
  - scan_code:
      oid: <<parameters.oid>>
      eid: <<parameters.eid>>
      api_key: <<parameters.api_key>>
      dir: <<parameters.dir>>
