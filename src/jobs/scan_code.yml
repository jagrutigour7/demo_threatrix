description: >
  Install threat agent
parameters:
  oid:
    type: string
    description: Organization ID
  eid:
    type: string
    description: Entity ID
  api_key:
    type: string
    description: API Key
  dir:
    type: string
    description: Path to scan the code
executor: default

steps:
  - scan_code:
      oid: <<parameters.oid>>
      eid: <<parameters.eid>>
      api_key: <<parameters.api_key>>
      dir: <<parameters.dir>>
