description: >
  Install threat agent
parameters:
  oid:
    type: string
    description: Organization_ID
  eid:
    type: string
    description: Entity_ID
  api_key:
    type: string
    description: API_Key
  dir:
    type: string
    description: Path to scan the code
executor: default

steps:
  - install_and_scan_code:
      oid: <<parameters.oid>>
      eid: <<parameters.eid>>
      api_key: <<parameters.api_key>>
      dir: <<parameters.dir>>
